import 'package:flutter/material.dart';

class NoTransactionContainer extends StatelessWidget {
  const NoTransactionContainer({super.key});

  @override
  Widget build(BuildContext context) {
    return Container();
  }
}
